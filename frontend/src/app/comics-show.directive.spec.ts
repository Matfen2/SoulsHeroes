import { ComicsShowDirective } from './comics-show.directive';

describe('ComicsShowDirective', () => {
  it('should create an instance', () => {
    const directive = new ComicsShowDirective();
    expect(directive).toBeTruthy();
  });
});
