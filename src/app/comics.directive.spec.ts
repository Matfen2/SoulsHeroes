import { ComicsDirective } from './comics.directive';

describe('ComicsDirective', () => {
  it('should create an instance', () => {
    const directive = new ComicsDirective();
    expect(directive).toBeTruthy();
  });
});
